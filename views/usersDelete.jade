extends layout

block content
  h1= title
  div
    form(method="post" action="/users/delete/submit")
      div Name:    
        select(name="name")
          option(value="" disabled selected) Which User?
          for user in body
            option(value=user.name) #{user.name}
      br
      input.entry(type ="button" onclick="location.href='../../'" value="Back")
      br
      input.entry(type="submit" value="Submit")